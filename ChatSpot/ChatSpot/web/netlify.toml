[build]
  command = "npm run build"
  publish = "dist"
  
[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  # This fixes the 404 on refresh for SPAs
  from = "/*"
  to = "/index.html"
  status = 200